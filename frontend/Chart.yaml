apiVersion: v2
name: frontend
description: Helm chart for PizzaShop Frontend (React SPA + nginx)
type: application
version: 0.1.0
appVersion: "1.0.0"
maintainers:
  - name: Sergei G.
    email: pr0g88

dependencies:
  # common-library — общие шаблоны (Deployment/Service/ServiceAccount/ConfigMap/HPA[/Ingress]).
  # См. ../common-library/templates/_*.tpl
  - name: common-library
    version: "0.1.0"
    repository: "file://../common-library"
