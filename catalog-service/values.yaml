replicaCount: 1

image:
  registry: ""
  repository: catalog-service
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: zot-registry-secret

serviceAccount:
  name: catalog-service-sa

service:
  type: ClusterIP
  port: 8082
  targetPort: 8082

resources:
  requests:
    memory: "64Mi"
    cpu: "100m"
  limits:
    memory: "128Mi"
    cpu: "200m"

probes:
  liveness:
    path: /health
    port: http
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  readiness:
    path: /ready
    port: http
    initialDelaySeconds: 10
    periodSeconds: 5
    timeoutSeconds: 3
    failureThreshold: 3

# HPA выключен намеренно — ограниченные ресурсы кластера (см. helm/README.md).
# Включён только у kitchen-service, где нагрузка неравномерная (пики заказов).
hpa:
  enabled: false
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

# Vault Agent Injector положит секреты в файлы /vault/secrets/<name> пода
# (см. common-library/templates/_deployment.tpl). Каждый элемент secrets —
# отдельный файл с перечисленными keys.
vault:
  enabled: true
  role: "pizzashop-dev"
  secretPath: "kv/pizzashop/dev/catalog"
  secrets:
    - name: db
      keys:
        - DB_USER
        - DB_PASSWORD

# Миграции
migrations:
  enabled: true
  type: "up"
  image:
    registry: ""
    repository: catalog-service-migrations
    tag: ""
    pullPolicy: IfNotPresent
  backoffLimit: 5
  resources:
    requests:
      memory: "64Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "100m"
  vault:
    enabled: true
    role: "pizzashop-dev"
    secretPath: "kv/pizzashop/dev/migrations"
  serviceAccount:
    name: "catalog-service-sa"
  databaseName: ""
  dbHost: ""
  dbPort: ""

configMap:
  SERVER_PORT: "8082"
  LOG_LEVEL: "info"
  DB_SSLMODE: "disable"